export enum LinkRoutes {
  HOME = "/",
  LOGIN = "/login",
  DASHBOARD = "/dashboard",
}

export enum Role {
  TEACHER = "Teacher",
  STUDENT = "Student",
  PRINCIPAL = "Principal",
}

export enum SideBarCurrent {
  Dashboard = "Dashboard",
  Teachers = "Teachers",
  Students = "Students",
  Exams = "Exams",
}

export enum Option {
  A = "A",
  B = "B",
  C = "C",
  D = "D",
}
